# function with example 


def avg(first, second, third):
    average = (first+ second + third) / 3
    return average
    
total_avg = avg(int(input("Enter the first value: ")),int(input("Enter the first value: ")),int(input("Enter the first value: ")))
print(f"Total average for given number : {total_avg}")
